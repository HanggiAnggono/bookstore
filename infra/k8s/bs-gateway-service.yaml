apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: bs-gateway
  name: bs-gateway
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
    - name: "9876"
      port: 9876
      targetPort: 9876
  selector:
    io.kompose.service: bs-gateway
